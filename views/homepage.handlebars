{{#each posts as |post|}}
<section class="card">
    {{!-- <a href="/posts/{{id}}"> --}}
        <header>
            <h4>{{post.title}}</h4>
            <h6>Posted by {{user.name}} at {{format_date post.created_at}}</h6>
        </header>
        <p>{{post.text}}</p>
        {{!--
    </a> --}}
</section>
{{/each}}